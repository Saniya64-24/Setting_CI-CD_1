mkdir config Core pages tests utils reports docker 

cd  docker
docker-compose up -d

download selenium latest 4.21 version

selenium imagesS  
every single node is single machine

in cmd :
docker --version
docker ps

Docker:
- selenium-hub
- chrome/node
- firefox

in docker
-container
click on currently created selenium hub
-then click on 444.4444 number below selenium hub --open desktop view